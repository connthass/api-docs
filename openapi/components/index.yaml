examples:
  $ref: ./examples.yaml

parameters:
  $ref: ./parameters.yaml

requestBodies:
  $ref: ./requestBodies.yaml

responses:
  $ref: ./responses.yaml
  
securitySchemes:
  ApiKeyAuth:
    type: apiKey
    in: header
    name: X-API-KEY
schemas:
  $ref: ./schemas.yaml
  
